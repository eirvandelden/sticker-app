<div>
  <header>
    <h1><%= t("parent.dashboard.title") %></h1>
  </header>

  <% @children.each do |child| %>
    <article id="<%= dom_id(child, :dashboard_row) %>"
             data-controller="confetti"
             data-confetti-completion-flag-id-value="<%= dom_id(child, :completion_flag) %>">
      <header>
        <% if child.user.avatar.attached? %>
          <%= image_tag child.user.avatar, size: "40x40", alt: child.user.name %>
        <% end %>
        <%= link_to parent_child_path(child) do %>
          <h2><%= child.user.name %></h2>
        <% end %>
      </header>

      <%= turbo_stream_from child %>

      <span id="<%= dom_id(child, :completion_flag) %>"></span>

      <%= render "parent/children/sticker_progress", card: child.display_sticker_card %>

      <% if child.rewardable_sticker_card %>
        <p id="<%= dom_id(child, :reward_badge) %>"><%= t("parent.dashboard.rewardable_cards", count: child.rewardable_sticker_cards_count) %></p>
      <% end %>

      <footer>
        <%= button_to t("parent.actions.give_sticker"), parent_child_sticker_path(child),
                      method: :post,
                      params: { emoji_mode: "random" },
                      class: "button-success" %>

        <%= button_to t("parent.actions.give_penalty"), parent_child_penalty_path(child),
                      method: :post,
                      class: "button-danger" %>
      </footer>
    </article>
  <% end %>

  <nav>
    <%= link_to t("parent.settings.index.title"), parent_settings_path %>
  </nav>

</div>
