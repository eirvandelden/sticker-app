---
# The rules shared by every personal Ruby project. This is a Rails app whose system tests
# drive a browser, so it stacks those two layers on top.
inherit_gem:
  rubocop-eirvandelden:
    - config/default.yml
    - config/rails.yml
    - config/capybara.yml
